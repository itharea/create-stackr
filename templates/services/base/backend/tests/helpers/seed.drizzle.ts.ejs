// Idempotent metadata seed — runs once per test suite from globalSetup.
// Populate as enum-like / lookup tables are added to the domain.
export async function seedMetadata(): Promise<void> {
  // No metadata yet; expand as needed.
}
